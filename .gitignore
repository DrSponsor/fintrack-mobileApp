
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
node_modules
.expo/
dist/
web-build/

# Local env overrides — never commit real secrets (Google OAuth client IDs,
# Paystack keys, Sentry DSN, PostHog key) that will eventually live here.
.env
.env.local
.env.*.local

# JVM crash artifacts from a Gradle build crash — not build output, not
# reproducible input, safe to regenerate/discard.
android/hs_err_pid*.log
android/replay_pid*.log


# Gradle output for local Expo modules. modules/ is source and IS committed —
# but each module compiles into its own android/build/, which is regenerated
# on every build and must not be.
modules/*/android/build/
