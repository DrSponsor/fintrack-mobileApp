/** @type {import('@react-native-community/cli-types').Config} */
module.exports = {
  dependencies: {
    // react-native-mmkv is now a real dependency — allow autolinking.
    // Previously excluded as a phantom transitive dependency.
  },
};
